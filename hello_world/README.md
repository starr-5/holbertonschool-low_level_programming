# Holberton Shell Project

This repository contains Bash scripts for the Shell project:

- `0-alias` → creates an alias
- `1-hello_you` → prints "hello" followed by the current user
- `2-path` → modifies the PATH environment variable
- `3-paths` → counts the number of directories in PATH
...
